// Re-export the configuration and utilities
export { locales, defaultLocale, localeConfig, namespaces } from './config';
export type { Locale, Namespace, TranslationNamespace } from './config';
export { Link, redirect, usePathname, useRouter } from './config';